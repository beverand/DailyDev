<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Questions</h1>
    <h2><%= user.userName %> has <%= total %> total, with <%= left %> answered.</h2>   
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
               <p> 
                    <span> <%= el.qtype %> </span>
                    <span class='<%= el.response ? 'completed' : 'not'%>'><%= el.todo %></span>
                    <span class='ans'> Answer </span>
                    <span class='rev'> Review </span>
                    <span class='<%= el.qsource == 'self' ? 'completed del' : 'hidden'%>'> Delete </span>
               </p>
                <textarea id="qAns" name="qAns" rows="5" cols="60" hidden><%= el.response %></textarea>
            </li>
    <% }) %>    
    </ul>

    <form action="/todos/createTodo" method='POST' class='<%= total < 200 ? 'completed' : 'hidden'%>'>
        <p><input type="text" placeholder="Enter Todo Item" name='todoItem'></p>
        <textarea id="newAns" name="newAns" rows="5" cols="40"></textarea>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>